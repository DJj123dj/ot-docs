---
title: /add
description: Add a user to a ticket.
sidebar_position: 11
keywords: [open-ticket, open ticket, discord ticket bot, discord ticket bot, reference, command]
---
<head>
    <link rel="apple-touch-icon" href="/img/logo_vertical.png"/>
</head>
import {C,M,ScaleImage,LinkBlock,InlineLink,FlexHorizontal,Divider,Hex} from "@site/src/components/openticket.jsx"

<FlexHorizontal>
    <ScaleImage src="/img/docs-v4/commands/ticket-add.png" alt="Example of the /add command." size="50%"></ScaleImage>
</FlexHorizontal>

## Overview
Using the `/add` command, you can add someone to a ticket. This will give the user the same permissions as the creator of the ticket.
You can remove someone who was added manually using the `/remove` command.
You can also provide an optional reason. This will be shown in the logs.

- **Default Permissions:** 🟠 Ticket Admins
- **Usable Locations:** 🔴 Ticket Only
- **Text Command:** `!ticket add <user> [reason]`
- **Slash Command:** `/add <user> [reason]`

## Parameters
- **`<user>`:** A user in the server to add to the ticket.
- **`[reason]`:** A reason for adding a user to the ticket. **<C color="green">(Optional)</C>**

## Settings
These are config variables which are related to this command.
- **[`general.json:"system"."emojiStyle"`](../config/general.mdx#system-emoji-style) <M color="blue">string</M>**
- **[`general.json:"system"."permissions"."add"`](../config/general.mdx#system-permissions) <M color="blue">string</M>**
- **[`general.json:"system"."messages"."adding"`](../config/general.mdx#system-messages) <M color="cyan">object</M>**