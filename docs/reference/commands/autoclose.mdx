---
title: /autoclose
description: Manage the autoclose system.
sidebar_position: 16
keywords: [open-ticket, open ticket, discord ticket bot, discord ticket bot, reference, command]
---
<head>
    <link rel="apple-touch-icon" href="/img/logo_vertical.png"/>
</head>
import {C,M,ScaleImage,LinkBlock,InlineLink,FlexHorizontal,Divider,Hex,ApiUrl,ApiBlock} from "@site/src/components/openticket.jsx"

<FlexHorizontal>
    <ScaleImage src="/img/docs-v4/commands/autoclose-disable.png" alt="Example of the /autoclose disable command." size="40%"></ScaleImage>
    <ScaleImage src="/img/docs-v4/commands/autoclose-enable.png" alt="Example of the /autoclose enable command." size="40%"></ScaleImage>
</FlexHorizontal>

## Overview
Using the `/autoclose` command, you can manage the autoclose system in a specific ticket.
You are able to disable it, (re-)enable it or change the inactivity time.
You can also provide an optional reason. This will be shown in the logs.

- **Default Permissions:** 🟠 Ticket Admins
- **Usable Locations:** 🔴 Ticket Only
- **Text Command:** `!ticket autoclose <disable|enable> <time> [reason]`
- **Slash Command:** `/autoclose <disable|enable> <time> [reason]`

## Parameters
- **`<disable|enable>`:** Enable or disable the autoclose system.
- **`<time>`:** Set the time the ticket needs to be inactive for when enabling the system. **<C color="green">(Only when using `enable`)</C>**
- **`[reason]`:** A reason for changing the autoclose system in the ticket. **<C color="green">(Optional)</C>**

## Settings
These are config variables which are related to this command.
- **[`options.json:"autoclose"`](../config/options.mdx#ticket-autoclose) <M color="cyan">object</M>**
- **[`general.json:"system"."emojiStyle"`](../config/general.mdx#system-emoji-style) <M color="blue">string</M>**
- **[`general.json:"system"."permissions"."autoclose"`](../config/general.mdx#system-permissions) <M color="blue">string</M>**
- **[`general.json:"system"."messages"."closing"`](../config/general.mdx#system-messages) <M color="cyan">object</M>**