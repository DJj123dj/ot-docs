---
title: /blacklist
description: Manage blacklisted users.
sidebar_position: 18
keywords: [open-ticket, open ticket, discord ticket bot, discord ticket bot, reference, command]
---
<head>
    <link rel="apple-touch-icon" href="/img/logo_vertical.png"/>
</head>
import {C,M,ScaleImage,LinkBlock,InlineLink,FlexHorizontal,Divider,Hex,ApiUrl,ApiBlock} from "@site/src/components/openticket.jsx"

<FlexHorizontal>
    <ScaleImage src="/img/docs-v4/commands/blacklist-view.png" alt="Example of the /blacklist view command." size="40%"></ScaleImage>
    <ScaleImage src="/img/docs-v4/commands/blacklist-remove.png" alt="Example of the /blacklist remove command." size="40%"></ScaleImage>
</FlexHorizontal>
<FlexHorizontal>
    <ScaleImage src="/img/docs-v4/commands/blacklist-add.png" alt="Example of the /blacklist add command." size="40%"></ScaleImage>
    <ScaleImage src="/img/docs-v4/commands/blacklist-get.png" alt="Example of the /blacklist get command." size="40%"></ScaleImage>
</FlexHorizontal>

## Overview
Using the `/blacklist` command, you can manage everything related to the user blacklist.
Add & remove users from the blacklist or take a look at all blacklisted users.
- **`view`:** View the current blacklist.
- **`get`:** Get details from a single user.
- **`add`:** Add a user to the blacklist. (With additional reason)
- **`remove`:** Remove a user from the blacklist. (With additional reason)

You can use these modes by inserting it as a subcommand.

- **Default Permissions:** 🟠 Ticket Admins
- **Usable Locations:** 🟠 Server (server & ticket)
- **Text Command:** `!ticket blacklist <view|get|add|remove> <user> [reason]`
- **Slash Command:** `/blacklist <view|get|add|remove> <user> [reason]`

## Parameters
- **`<view|get|add|remove>`:** Choose what to do with the blacklist system.
- **`<user>`:** The user to modify/view. **<C color="green">(Only when using `get`, `add` or `remove`)</C>**
- **`[reason]`:** A reason for changing the blacklist. **<C color="green">(Only when using `add` or `remove`)</C>**

## Settings
These are config variables which are related to this command.
- **[`options.json:"allowCreationByBlacklistedUsers"`](../config/options.mdx#ticket-allow-blacklisted) <M color="red">boolean</M>**
- **[`general.json:"system"."emojiStyle"`](../config/general.mdx#system-emoji-style) <M color="blue">string</M>**
- **[`general.json:"system"."permissions"."blacklist"`](../config/general.mdx#system-permissions) <M color="blue">string</M>**
- **[`general.json:"system"."messages"."blacklisting"`](../config/general.mdx#system-messages) <M color="cyan">object</M>**